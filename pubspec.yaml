name: flutter_tdd_clean_architeture
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^2.0.1
  flutter_bloc: 3.0.0
  equatable: ^1.0.1
  dartz: ^0.9.1
  connectivity: ^0.4.3+7
  http: ^0.12.0+2
  shared_preferences: ^0.5.3+4

  data_connection_checker: ^0.3.4

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.0

flutter:
  uses-material-design: true
